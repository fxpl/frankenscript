# Do threads dynamically release cowns?
def testA(a):
    r = Region()
    b = Cown(r)
    @name("B1")
    when(b):
        b.value = Region()
    if is_executable_or_complete("B1") == True:
        unreachable()
    r = None
    if is_executable_or_complete("B1") == False:
        unreachable()

a = Cown(Region())
t1 = Thread(testA, a)
start(t1)
pass()