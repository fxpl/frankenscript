def object():
    def str_name(self):
        return "Dictionary"

    value = {}
    value.name = str_name

    return value

# Method on an object
obj = object()
obj.name()
drop obj

# Stand alone call
object().name()

# Assignment
name = object().name()
