global = {}

def add_cown(obj, name):
    a = {}
    a.name = name
    region(a)
    obj[name] = cown(move a)

add_cown(global, "c1")
add_cown(global, "c2")
add_cown(global, "c3")
add_cown(global, "c4")
add_cown(global, "c5")
add_cown(global, "c6")



drop global
